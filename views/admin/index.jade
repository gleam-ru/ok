extends ../_layouts/no-title.jade

block append head
  //- +globalVar('users', users)
  //- +globalVar('roles', roles)
  script(src='/ViewControllers/admin/index.js')
  style.
    


block content
  section
    .container-non-responsive
      .row
        h2 Users
        ul.flat-list.text-med
          li
            a(href='/admin/users') total list
          li
            a(href='/admin/qa') user messages (#{qas})
          li
            a(href='/admin/requests') pay requests (#{pay_requests})
          li
            a(href='/admin/contracts/account_application') Account applications (#{account_applications})
          li
            a(href='/admin/contracts/credit_line') Credit lines (#{credit_lines})
